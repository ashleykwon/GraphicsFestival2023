{
  "name": "graphicsfestival2023",
  "version": "1.0.0",
  "description": "1. Run `http-server -c-1` in root directory to start up local HTTP server to server Three.js webpage\r 2. Open `/js/index.html` in the browser and *wait* for CDN to finish downloading Three.js library files\r 3. Music graphics time!",
  "main": "./dist/bundle.js",
  "scripts": {
    "build": "webpack --config webpack.config.js --watch",
    "run": "http-server -c-1"
  },
  "devDependencies": {
    "ts-loader": "^9.4.2",
    "typescript": "^4.9.4",
    "webpack": "^5.75.0",
    "webpack-cli": "^5.0.1"
  },
  "dependencies": {
    "@types/three": "^0.155.1",
    "three": "^0.155.0"
  },
  "author": "",
  "license": "ISC"
}
